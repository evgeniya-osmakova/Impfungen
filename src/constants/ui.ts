export const HTML_BUTTON_TYPE = {
  button: 'button',
  submit: 'submit',
} as const;

export const HTML_INPUT_TYPE = {
  date: 'date',
  number: 'number',
  search: 'search',
  text: 'text',
} as const;

export const HTML_ROLE = {
  alert: 'alert',
  group: 'group',
} as const;

export const ARIA_LIVE = {
  assertive: 'assertive',
} as const;

export const HTML_TAG = {
  button: 'button',
} as const;

export const BUTTON_VARIANT = {
  danger: 'danger',
  primary: 'primary',
  secondary: 'secondary',
} as const;

export const RADIO_PILL_GROUP_SIZE = {
  compact: 'compact',
  default: 'default',
} as const;
